name: Deploy Documenso
on:
  push:
    branches:
      - mm/main
      - mm/staging
jobs:
  deploy_infra:
    # Only run on the original repository, not forks
    if: ${{ github.repository == 'monetarymetals/documenso' }}
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [22]
    environment:
      name: ${{ github.ref_name }}
    steps:
      # ------------ Setup Environment ------------
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      # ------------ Deploy Infrastructure ------------
      - name: Set AWS Creds
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-region: ${{ vars.AWS_REGION }}
          role-to-assume: ${{ vars.DEPLOY_ROLE }}
          aws-access-key-id: ${{ vars.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}

      - name: Deploy Infrastructure
        run: npm run sst:deploy -- --stage ${{ vars.STAGE }}
        env:
          AWS_REGION: ${{ vars.AWS_REGION }}
          NEXT_PRIVATE_SMTP_FROM_ADDRESS: ${{ vars.NEXT_PRIVATE_SMTP_FROM_ADDRESS }}
          NEXT_PRIVATE_SMTP_TRANSPORT: ${{ vars.NEXT_PRIVATE_SMTP_TRANSPORT }}
          NEXT_PRIVATE_SMTP_FROM_NAME: ${{ vars.NEXT_PRIVATE_SMTP_FROM_NAME }}
          NEXT_PUBLIC_DISABLE_SIGNUP: ${{ vars.NEXT_PUBLIC_DISABLE_SIGNUP }}
          NEXT_PUBLIC_DOCUMENT_SIZE_UPLOAD_LIMIT: ${{ vars.NEXT_PUBLIC_DOCUMENT_SIZE_UPLOAD_LIMIT }}
